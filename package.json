{
  "name": "homeshopview-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply homeshopview-db --local",
    "db:migrate": "wrangler d1 migrations apply homeshopview-db --remote",
    "db:seed": "node scripts/make-seed-sql.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
